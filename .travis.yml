#
#
#
language: cpp

before_script:
  - sudo apt-get update && sudo apt-get install -y libboost-all-dev 
  - mkdir build

script: 
  - autoreconf -i
  - cd build
  - sh ../configure --prefix=`pwd`
  - make -j16
  - make install 
