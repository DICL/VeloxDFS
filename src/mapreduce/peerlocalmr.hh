#pragma once
#include "../common/peerlocal.hh"

namespace eclipse {

class PeerLocalMR: public PeerLocal {
  public:
    PeerLocalMR (Context&);
    ~PeerLocalMR ();

  private:
};

} /* eclipse  */ 
